[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# Gate CI on real problems (undefined names, unused imports, syntax/errors,
# basic style) rather than stylistic churn across the existing codebase.
select = ["E4", "E7", "E9", "F", "W605"]
